import React from 'react';
import NotificationElement from './NotificationElement/NotificationElement';

const notification = props => {

    const notificationsElements = '';

    return (
        'NOTIFYCATION BOILERPLATE'
    )
}

export default notification;